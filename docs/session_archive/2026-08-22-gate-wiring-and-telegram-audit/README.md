# Архив сессии — 2026-08-21/22

Сжатый контекст одной длинной сессии Claude Code над `Why_Ai`, записанный
перед исчерпанием окна. Не стенограмма — только то, что дорого потерять:
решения, реальные баги, фактическое состояние.

**Порядок чтения:** этот файл → `decisions.md` → `state.md`.
**Для инъекции в новую сессию:** вставьте `README.md` + `decisions.md`
первым сообщением; `state.md` — по необходимости, для конкретного трека.

## Сводка в 7 пунктах

1. Починен MCP-сервер `why-ai-mcp` (реализация MCP-хендшейка и схемы) и
   зарегистрирован в правильном project-scope (`Why_Ai`, не `_Ai`).
2. Построен control-plane ai-loop (`.ai-loop/policy/*`, вендоренный
   классификатор, CODEOWNERS, CI) — старая версия была на несовместимой схеме.
3. Аудит `dVPN` (Фаза 4) и предложенной Telegram-спецификации: dVPN —
   детерминированный симулятор без сети; Telegram-спецификация была
   нереализуема как есть (синтаксическая ошибка, несуществующие эндпоинты,
   требовала правки зоны R). Вместо неё построена своя архитектура
   (SSE-мост, без правки `Supervisor/`) — `docs/final_vision/`.
4. Реализованы и запушены: реальный Telegram-транспорт с fail-closed
   авторизацией, Mini App, фикс лживого /panic в дашборде.
5. **Отдельный, более серьёзный аудит: цикл самоэволюции Why_Ai разомкнут —
   работает 1 звено из 9** (нет ни предложителя, ни применителя изменений).
   `docs/final_vision/06-self-evo-audit.md`.
6. По итогам — вручную (без subagent, из-за лимита API) подключены реальные
   гейты: храповики размера к `CommitGate`, контракт `IdempotencyGate`,
   SIGTERM для `[prod_evo]`, единственный источник истины режима (YAML).
7. Git-инцидент: два коммита ушли не на ту ветку после мержа PR —
   обнаружено, исправлено cherry-pick’ом, ничего не потеряно.

## Иерархия истины (не меняется этим архивом)

| Уровень | Что | Полномочия |
|---|---|---|
| 1 | `.ai-loop/policy/*` | обязательна, проверяется классификатором |
| 2 | `Supervisor/Constitution/BIBLE.md` | конституция, 15 принципов |
| 3 | `docs/final_vision/` | текущий план Telegram-цикла |
| 4 | этот архив (`docs/session_archive/`) | снимок состояния на дату, не источник правил |
| 5 | `docs/*.md` (прочее) | предложения и история, НЕ источник правил |

## Git на момент архивации

Ветка `main`, синхронна с `origin/main`, последний коммит
`11f37b3` (2026-08-22). Рабочее дерево чисто. 157/157 тестов,
`run_mvp.py --self-test` → 0, оба классификатора → `ok: true`.
